# Use hoisted node linker for maximum compatibility with packages
# that expect flat node_modules (e.g. ESLint plugins, Next.js internals)
node-linker=hoisted

# Ensure workspace packages are linked correctly
link-workspace-packages=true
